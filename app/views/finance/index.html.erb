<div id="content-header">
  <img src="/images/show_finance.png" alt=""/>

  <h1><%= t('finance.title') %></h1>

  <h3><%= t("manage") %> <%= t('finance.title') %></h3>

  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png", :border => 0), "history.back()" %>
  </div>
</div>
<div id="page-yield">
  <% unless flash[:notice].nil? %>
      <p class="flash-msg"><%= flash[:notice] %></p>
  <% end %>

  <div class="box">

    <div class="link-box">
      <div class="link-heading"><%= link_to t("fees"), :action => "fees_index" %> </div>
      <div class="link-descr"><%= t("finance.manageFees") %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to t("category"), :action => "categories" %> </div>
      <div class="link-descr"><%= t("manage") %> <%= t("category") %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to t("transactions"), :action => "transactions" %> </div>
      <div class="link-descr"><%= t("creates") %> <%= t("expensesAndIncome") %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to t("donations"), :action => "donation" %></div>
      <div class="link-descr"><%= t('descDonations') %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to t('autoTransactions'), :action => "automatic_transactions" %></div>
      <div class="link-descr"><%= t('descAutoTransactions') %></div>
    </div>
    <% unless @hr.nil? %>
        <div class="link-box">
          <div class="link-heading"><%= link_to t("payslip"), :action => "payslip_index" %></div>
          <div class="link-descr"><%= t('employee.payslip') %></div>
        </div>
    <% end %>

    <div class="link-box">
      <div class="link-heading"><%= link_to t('finance.assetLiability'), :action => "asset_liability" %></div>
      <div class="link-descr"><%= t('finance.assetLiability') %></div>
    </div>

    <div class="link-box">
      <div class="link-heading"><%= link_to t('export'), :controller => 'xml', :action => "index" %></div>
      <div class="link-descr"> <%= t('descExportXML') %></div>
    </div>

  </div>
</div>